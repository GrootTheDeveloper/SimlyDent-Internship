# Projects

Mỗi dự án công ty được clone vào một thư mục riêng và giữ nguyên Git repository của dự án đó.

```text
projects/
├── simlydent-frontend/
├── simlydent-backend/
└── other-service/
```

Không commit mã nguồn dự án vào `internship-log`.
