# Local PoC Run — 2026-08-05

## Environment

| Trường | Giá trị |
|---|---|
| Host | Windows, Docker Desktop 4.47.0 |
| Docker Engine | 28.4.0, Linux containers |
| Docker Compose | 2.39.4-desktop.1 |
| LiveKit Server | 1.13.1 |
| Backend build | .NET SDK/runtime 10 container images |
| Frontend | Vue 2.7.16, livekit-client 2.21.0, SignalR 10.0.11, Vite 7.3.6 |

## Automated result

Command: `scripts/smoke-test.ps1`

| Check | Expected | Actual |
|---|---:|---:|
| Cross-tenant create A1 → B1 | 403 | PASS |
| Same-tenant create A1 → A2 | 201 | PASS |
| Reconcile active call as A2 | 200 | PASS |
| Token before accept | 409 | PASS |
| Cross-tenant read by B1 | 404 | PASS |
| First accept by A2 | 200 | PASS |
| Duplicate accept | 409 | PASS |
| Token after accept | 200 | PASS |
| JWT room/identity grants | scoped | PASS |
| End accepted call | 200 | PASS |
| Duplicate end | 409 | PASS |
| Create A2 → A3 | 201 | PASS |
| Accept A2 ↔ A3 | 200 | PASS |
| Busy A1 → A2 | 409 | PASS |
| End A2 ↔ A3 | 200 | PASS |

Kết quả: **15/15 PASS**.

## Browser verification

- Trang A1 render và SignalR chuyển sang `Realtime online`.
- A1 tạo call tới A2; caller hiển thị `Ringing` và action `Hủy cuộc gọi`.
- A2 mở sau khi invitation đã phát; endpoint reconcile khôi phục đúng call `Ringing` sau refresh.
- A2 từ chối; UI chuyển sang `Rejected` và hiển thị action tạo cuộc gọi mới.

Lần chạy ban đầu chưa cấp quyền camera/microphone. Kiểm tra thiết bị thật được thực hiện sau đó và ghi riêng bên dưới.

Kiểm tra bổ sung sau khi triển khai HTTPS và responsive:

- HTTPS UI và `/api/identities` qua Cloudflare Quick Tunnel trả HTTP 200.
- Backend phát đúng LiveKit URL dạng `wss://<quick-tunnel-host>`.
- LiveKit WebSocket kết nối thành công và trả frame signaling đầu tiên.
- SignalR negotiate qua tunnel trả transport WebSockets.
- Viewport 390x844 và 360x800 không phát sinh tràn ngang hoặc tràn chiều cao.
- Kiểm tra viewport không thay thế kiểm thử camera/microphone trên Android hoặc iOS thật.

## Kiểm tra laptop và iPhone

- Hai thiết bị truy cập cùng liên kết HTTPS qua Cloudflare Quick Tunnel.
- Laptop và iPhone cùng vào một phòng LiveKit; server ghi nhận cả hai participant ở trạng thái active.
- Cả hai phía phát track audio và video VP8. iPhone cấp quyền camera/microphone thành công.
- Laptop nhận và hiển thị video dọc từ iPhone.
- Client đã được sửa để đăng ký chủ động track từ xa, gắn lại track có sẵn và hỗ trợ inline playback trên iOS.
- Video dọc được hiển thị theo tỷ lệ gốc, co theo chiều cao vùng gọi và không crop.
- Chưa ghi nhận lần nghiệm thu cuối cho A/V hai chiều sau bản sửa; không đánh dấu hạng mục này là đạt.
- Hai thiết bị dùng cùng mạng Windows Mobile Hotspot. Kết quả không đại diện cho kết nối Internet khác mạng.

## Service verification

- Năm service được cấu hình: `livekit`, `backend`, `frontend`, `gateway` và `cloudflared`; `cloudflared` chỉ chạy trong profile chia sẻ demo.
- LiveKit bind HTTP 7880, RTC TCP 7881 và UDP 50000–50020.
- LiveKit quảng bá IP LAN của host cho media; cấu hình hiện tại không hỗ trợ media khác mạng.
- API secret được sinh ngẫu nhiên vào `.env` ngoài source control; startup log không có cảnh báo secret quá ngắn.
- `npm audit` qua registry npm chính thức: 0 critical, 0 high, 0 moderate, 1 low; advisory còn lại thuộc Vue 2.7.16 và chỉ có fix bằng Vue 3.

## Qualification status

| Nhóm | Trạng thái |
|---|---|
| Build/reproducibility local | PASS |
| Application state/tenant hard gates | PASS trong phạm vi smoke test |
| Refresh/reconcile ringing | PASS |
| Media thiết bị thật | PARTIAL — hai phía publish, laptop nhận video iPhone; chưa nghiệm thu hai chiều |
| Forced TURN/relay | NOT RUN — chưa triển khai coturn/public host |
| Responsive mobile viewport | PASS tại 360x800 và 390x844 |
| Android/iOS thiết bị thật | iPhone PARTIAL; Android NOT RUN |
| Reconnect/raw WebRTC stats | NOT RUN |

## UI scope

- Trang chính quản lý danh sách người dùng và thao tác bắt đầu cuộc gọi.
- Popup lời mời, chấp nhận/từ chối và cửa sổ `/call/{callId}` riêng đã được triển khai.
- Màn hình gọi có video từ xa, video cục bộ, bật/tắt camera, microphone và kết thúc cuộc gọi.
