# Refactor evidence ? production call/media

**Date:** 2026-08-07  
**Branch:** `refactor/production-call-media`  
**Final HEAD (Phase 8 doc):** `f5e4a92` (+ this commit)  
**Rollback (pre-refactor lab main):** `b9a80d2`  
**VPS:** `https://103.28.32.118.sslip.io` (2 vCPU lab)

## Commits (phase trail)

| Phase | Commit (approx) | Summary |
|-------|-----------------|---------|
| P1 | `0c3b2bf` | shared auth/api/constants/helpers |
| P2 | `dabd516` / `cf299c2` | MediaEngine modules + wire call window |
| P3 | `ca2ec8c` | portal / call-window split; thin main.js |
| P4 | `f9b851c` | Program.cs ? Endpoints/* |
| P5 | `d1ab6b0` | legacy vs canonical media paths |
| P6 | `cbc19df` | embed media-primitives |
| P7 | `f5e4a92` | typed Options + safe-log |
| P8 | (this) | architecture + regression evidence |

## Build / deploy

- Frontend: Vite multi-chunk (portal / call-window) + static widget files ? built on VPS Docker.
- Backend: .NET publish in Docker; `Options OK` logged at startup without secrets.
- Deploy: `docker compose -f docker-compose.vps.yml up -d --build backend frontend` (affected services only).

## Regression R1?R14 (2026-08-07 VPS automated + manual marks)

Source: `evidence/regression-r1-r14-20260807.txt`

| ID | Case | Result | Notes |
|----|------|--------|-------|
| R1 | Audio initialMediaMode authority | **PASS** | A1 create + A2 GET = Audio |
| R2 | Video initialMediaMode authority | **PASS** | create returns Video |
| R3 | Audio?Video toggle ?5 | **MANUAL** | Browser both sides |
| R4 | Both sides toggle | **MANUAL** | Browser |
| R5 | Reopen call window | **MANUAL** | Focus-only; no sendBeacon end |
| R6 | Reconnect media | **MANUAL** | Disconnected ? End |
| R7 | Visitor/embed remote audio | **PASS*** | Widget assets 200; Safari play = browser |
| R8 | Intentional hangup | **PASS** | Accept ? end 200 |
| R9 | Auto CallAudio | **PASS** | media_assets `CallAudio\|Recording` after consent |
| R10 | Dental without camera | **PASS** | HTTP 409 guard |
| R11 | Snapshot lifecycle | **MANUAL** | Needs live patient camera |
| R12 | Manager recordings list | **PASS** | GET /api/recordings 200 |
| R13 | Refresh/reopen UI | **MANUAL** | Browser |
| R14 | Backend restart survival | **DOCUMENTED** | In-memory CallSession lost; GET may 404 after restart; brief 502 while container boots |

\*R7 full audio playback is browser-dependent (Safari autoplay).

## Legacy classification

| Component | Class | Replacement | Remove when |
|-----------|-------|-------------|-------------|
| IRecordingCatalog / recordings table | DEPRECATED | IConsultationCatalog media_assets | All UI on media model |
| RecordingOrchestrationService | DEPRECATED | ConsultationAudio / Dental / Snapshot services | Legacy API removed |
| Finalize ApplyEgressStatusAsync | DEPRECATED path | ApplyMedia* | Legacy catalog gone |
| Reconcile legacy loop | DEPRECATED | RunCanonicalMediaReconcileAsync | Legacy catalog gone |
| /api/calls/{id}/recording/* | KEEP compat | media-first APIs | Staff UI migrated |
| /api/recordings | KEEP compat | consultations UI | Manager UI migrated |

## Remaining debt

1. **CallSession not durable** ? restart drops ringing/active calls (R14).
2. **2 vCPU egress concurrency** ? capacity, not code.
3. **Browser manual suite** R3?R6, R11, R13 still required before merge to main.
4. **AuthTokenService** still partly raw IConfiguration (Options Auth bound but not fully injected).
5. **Embed** IIFE duplicated with ESM primitives ? keep @shared-pair in sync.
6. Demo credentials / open CORS staff policy ? PoC only.

## Secrets policy

Evidence must not include passwords, JWTs, S3 keys, or full presigned URLs.
