log_level: info
ws_url: ws://livekit:7880
insecure: true
redis:
  address: redis:6379
backup_storage: /out
health_port: 8080
prometheus_port: 9090

# LAB ONLY — scheduler admission accounting, NOT real encode cost reduction.
# Do not lower further to "fix" capacity. Concurrent CallAudio + Dental/Video
# on this 2 vCPU host remains an INFRA CAPACITY risk (Chrome/ffmpeg still heavy).
# Historical failure under real load: "no response from servers".
# Product concurrency requires dedicated Egress worker or ≥4 vCPU node.
cpu_cost:
  room_composite_cpu_cost: 0.3
  web_cpu_cost: 0.3
  track_composite_cpu_cost: 0.25
  track_cpu_cost: 0.15
